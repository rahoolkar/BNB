<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BNB</title>
</head>
<body>
    <h3>Listing Details : <%= data._id %> </h3>
    <ul>
        <li><%= data.title %></li>
        <li><%= data.description %></li>
        <li>&#8377; <%= data.price.toLocaleString('en-IN') %></li>
        <li><%= data.location %></li>
        <li><%= data.country %></li>
    </ul>
    <a href="/listings/<%= data._id %>/edit">Edit this Listing</a><br>
    <form action="/listings/<%= data._id %>?_method=DELETE" method="post">
        <button>Delete</button>
    </form>
</body>
</html>